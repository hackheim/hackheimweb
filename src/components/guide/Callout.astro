---
const { type = "info" } = Astro.props;

const styles = {
  info: {
    border: "border-blue-300",
    bg: "bg-blue-50",
    icon: "ℹ️",
    iconBg: "bg-blue-500"
  },
  warning: {
    border: "border-amber-300",
    bg: "bg-amber-50",
    icon: "⚠️",
    iconBg: "bg-amber-500"
  },
  danger: {
    border: "border-red-300",
    bg: "bg-red-50",
    icon: "🚨",
    iconBg: "bg-red-500"
  }
};

const style = styles[type] || styles.info;
---
<div class={`callout my-4 rounded-xl border ${style.border} ${style.bg} p-4`}>
  <div class="flex items-start gap-3">
    <div class={`callout-icon flex-shrink-0 w-8 h-8 ${style.iconBg} text-white rounded-full flex items-center justify-center text-sm`}>
      {style.icon}
    </div>
    <div class="callout-content flex-1 text-sm leading-relaxed">
      <slot />
    </div>
  </div>
</div>