---
const { type = "info" } = Astro.props;

const styles = {
  info: {
    border: "border-blue-300",
    bg: "bg-blue-50",
    icon: "ℹ️",
  },
  warning: {
    border: "border-amber-300",
    bg: "bg-amber-50",
    icon: "⚠️",
  },
  danger: {
    border: "border-red-300",
    bg: "bg-red-50",
    icon: "🚨",
  }
};

const style = styles[type] || styles.info;
---
<div class={`callout my-4 rounded-xl border ${style.border} ${style.bg} px-4 py-3`}>
  <div class="flex items-start gap-3">
    <div class={`callout-icon flex-shrink-0 w-5 h-5 flex items-center justify-center text-base leading-none mt-0.5`}>
      {style.icon}
    </div>
    <div class="callout-content flex-1 text-sm leading-relaxed [&>p]:m-0">
      <slot />
    </div>
  </div>
</div>
